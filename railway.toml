[build]
  builder = "DOCKERFILE"
  dockerfilePath = "Dockerfile"

[deploy]
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 5
  # Railway healthcheck (optional)
  # healthcheckPath = "/"

[deploy.env]
  # اگه بخوای پورت DNS رو تغییر بدی:
  # DNS_PORT = "53"
